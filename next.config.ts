import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Desactivar Turbopack temporalmente si hay problemas de compilación
  // experimental: {
  //   turbo: false,
  // },
};

export default nextConfig;
